import styled from 'styled-components';

export const CaptureDeliveryWrapper = styled.div`
  width: 100vw;
  height: 100vh;
  padding-top: 500px;
  box-sizing: border-box;
`;

export const CaptureDeliveryHand = styled.img`
  position: absolute;
  top: 0;
  right: 0;
`;
